<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LabourCinnect1" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="android:statusBarColor">#429E9D</item>

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LabourCinnect1" parent="Base.Theme.LabourCinnect1" />
    <style name="NavigationItemStyle">
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/poppins_bold</item>
        <item name="android:background">@color/red</item>

        <!-- Add more style attributes as needed -->
    </style>
    <style name="NavigationItemTextStyle">
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Body1</item>
        <item name="android:fontFamily">@font/poppins_bold</item>
        <!-- Add more style attributes as needed -->
    </style>
    <style name="SectionTitleStyle">
        <item name="android:textColor">@color/red</item>
        <item name="android:fontFamily">@font/medium_font</item>
        <!-- Add more style attributes as needed -->
    </style>

    <style name="labourConnectstyle">
        <item name="android:textColor">@color/brown</item>
        <item name="android:fontFamily">@font/medium_font</item>
        <item name="android:textSize">26sp</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Body1</item>

        <!-- Add more style attributes as needed -->
    </style>
</resources>